[project]
name = "inference-lab"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "safetensors>=0.8.0",
    "torch>=2.14.0",
    "transformers>=5.17.0",
]
